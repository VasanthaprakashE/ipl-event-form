services:
  - type: web
    name: ipl-event-dashboard
    runtime: python
    plan: free
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:application --bind 0.0.0.0:$PORT
    envVars:
      - key: FLASK_ENV
        value: production